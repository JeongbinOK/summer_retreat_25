<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>내 팀 - 2025 말씀교회 여름수련회</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body { 
            font-family: 'Noto Sans KR', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
        }
        .navbar-brand { font-weight: bold; }
        .balance-badge { font-size: 1.1em; }
        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.95);
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.15);
        }
        .card-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 15px 15px 0 0 !important;
            border: none;
            padding: 20px;
        }
        .btn {
            border-radius: 10px;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }
    </style>
</head>
<body class="bg-light">
    <%- include('../partials/navbar', { user: user }) %>

    <main class="container mt-4">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">
                            <% if (teamInfo) { %>
                                <%= teamInfo.name %>
                            <% } else { %>
                                팀이 지정되지 않음
                            <% } %>
                        </h5>
                        <% if (teamInfo) { %>
                            <div class="btn-group" role="group">
                                <a href="/user/team-inventory" class="btn btn-primary btn-sm">
                                    <i class="fas fa-boxes me-1"></i>팀 인벤토리
                                </a>
                                <a href="/user/team-purchases" class="btn btn-outline-primary btn-sm">
                                    <i class="fas fa-history me-1"></i>구매 내역
                                </a>
                            </div>
                        <% } %>
                    </div>
                    <div class="card-body">
                        <% if (!teamInfo) { %>
                            <div class="text-center py-4">
                                <i class="fas fa-users fa-3x text-muted mb-3"></i>
                                <p class="text-muted">아직 팀에 지정되지 않았습니다. 관리자에게 문의해주세요.</p>
                            </div>
                        <% } else { %>
                            <div class="row">
                                <% teamMembers.forEach(member => { %>
                                    <div class="col-md-6 col-lg-4 mb-3">
                                        <div class="card <%= member.id === user.id ? 'border-primary' : '' %>">
                                            <div class="card-body">
                                                <h6 class="card-title">
                                                    <%= member.username %>
                                                    <% if (member.id === user.id) { %>
                                                        <span class="badge bg-primary">나</span>
                                                    <% } %>
                                                    <% if (member.role === 'team_leader') { %>
                                                        <span class="badge bg-success"><i class="fas fa-crown me-1"></i>리더</span>
                                                    <% } %>
                                                </h6>
                                                <p class="card-text">
                                                    <small class="text-muted"><i class="fas fa-coins me-1"></i>잔액: $<%= member.balance %></small>
                                                </p>
                                            </div>
                                        </div>
                                    </div>
                                <% }); %>
                            </div>
                            
                            <div class="mt-4">
                                <h6><i class="fas fa-chart-line me-2"></i>팀 재정 요약</h6>
                                <div class="row">
                                    <div class="col-md-3">
                                        <div class="card bg-success text-white">
                                            <div class="card-body text-center">
                                                <h5>$<%= financialData.earned + financialData.received %></h5>
                                                <small>총 수입</small>
                                                <div class="mt-1">
                                                    <small>코드: $<%= financialData.earned %></small><br>
                                                    <small>기부받음: $<%= financialData.received %></small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-3">
                                        <div class="card bg-danger text-white">
                                            <div class="card-body text-center">
                                                <h5>$<%= financialData.spent %></h5>
                                                <small>총 지출</small>
                                                <div class="mt-1">
                                                    <small>구매</small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-3">
                                        <div class="card bg-warning text-dark">
                                            <div class="card-body text-center">
                                                <h5>$<%= financialData.donated %></h5>
                                                <small>총 기부</small>
                                                <div class="mt-1">
                                                    <small>다른 팀에게</small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-3">
                                        <div class="card bg-primary text-white">
                                            <div class="card-body text-center">
                                                <h5>$<%= financialData.currentBalance %></h5>
                                                <small>현재 잔액</small>
                                                <div class="mt-1">
                                                    <small>사용 가능</small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="mt-4">
                                <h6><i class="fas fa-users me-2"></i>팀원</h6>
                                <div class="row">
                                    <div class="col-md-4">
                                        <div class="card bg-light">
                                            <div class="card-body text-center">
                                                <h5><%= teamMembers.length %></h5>
                                                <small>Total Members</small>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-4">
                                        <div class="card bg-light">
                                            <div class="card-body text-center">
                                                <h5>$<%= Math.round(financialData.currentBalance / teamMembers.length) %></h5>
                                                <small>Average Balance</small>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-4">
                                        <div class="card bg-light">
                                            <div class="card-body text-center">
                                                <h5><%= teamMembers.filter(m => m.role === 'team_leader').length %></h5>
                                                <small>Team Leaders</small>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Received Donations Section -->
                            <% if (receivedDonations && receivedDonations.length > 0) { %>
                            <div class="mt-4">
                                <h6>🎁 Recent Donations Received</h6>
                                <div class="row">
                                    <% receivedDonations.forEach(donation => { %>
                                        <div class="col-md-6 mb-3">
                                            <div class="card border-info">
                                                <div class="card-body">
                                                    <div class="d-flex justify-content-between align-items-start">
                                                        <h6 class="card-title text-info">
                                                            <%= donation.product_name %> x<%= donation.quantity %>
                                                        </h6>
                                                        <span class="badge bg-info">$<%= donation.amount %></span>
                                                    </div>
                                                    <p class="mb-1">
                                                        <strong>From:</strong> <%= donation.donor_team_name %> 
                                                        (<%= donation.donor_username %>)
                                                    </p>
                                                    <% if (donation.message && donation.message.trim()) { %>
                                                        <div class="alert alert-light p-2 mt-2">
                                                            <small><em>"<%= donation.message %>"</em></small>
                                                        </div>
                                                    <% } %>
                                                    <small class="text-muted">
                                                        Received: <%= formatDate(donation.created_at) %>
                                                    </small>
                                                </div>
                                            </div>
                                        </div>
                                    <% }); %>
                                </div>
                                <div class="text-center">
                                    <small class="text-muted">
                                        Showing last 10 donations • Check <a href="/user/team-inventory">Team Inventory</a> for all items
                                    </small>
                                </div>
                            </div>
                            <% } %>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        async function logout() {
            try {
                const response = await fetch('/auth/logout', { method: 'POST' });
                if (response.ok) {
                    window.location.href = '/login';
                }
            } catch (error) {
                console.error('Logout error:', error);
            }
        }
    </script>
</body>
</html>