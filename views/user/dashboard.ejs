<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>대시보드 - 2025 말씀교회 여름수련회</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700&family=Noto+Serif+KR:wght@400;500;600&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body { 
            font-family: 'Noto Sans KR', sans-serif; 
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
        }
        .navbar-brand { font-weight: bold; }
        .balance-badge { 
            font-size: 1.3em; 
            background: linear-gradient(45deg, #28a745, #20c997);
            border: none;
            box-shadow: 0 4px 15px rgba(40, 167, 69, 0.3);
        }
        .card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.95);
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.15);
        }
        .card-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 15px 15px 0 0 !important;
            border: none;
            padding: 20px;
        }
        .transaction-item { 
            border-left: 4px solid #007bff; 
            padding: 15px; 
            margin-bottom: 15px; 
            border-radius: 0 10px 10px 0;
            background: rgba(248, 249, 250, 0.5);
            transition: all 0.3s ease;
        }
        .transaction-item:hover {
            background: rgba(248, 249, 250, 0.8);
            transform: translateX(5px);
        }
        .transaction-earn { 
            border-left-color: #28a745;
            background: rgba(40, 167, 69, 0.1);
        }
        .transaction-spend { 
            border-left-color: #dc3545;
            background: rgba(220, 53, 69, 0.1);
        }
        .welcome-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 20px;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }
        .btn {
            border-radius: 10px;
            font-weight: 500;
            transition: all 0.3s ease;
            border: none;
        }
        .btn-primary {
            background: linear-gradient(45deg, #007bff, #0056b3);
        }
        .btn-success {
            background: linear-gradient(45deg, #28a745, #20c997);
        }
        .btn-outline-primary {
            border: 2px solid #007bff;
            color: #007bff;
        }
        .btn-outline-primary:hover {
            background: linear-gradient(45deg, #007bff, #0056b3);
            transform: translateY(-2px);
        }
        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }
        .alert {
            border: none;
            border-radius: 10px;
        }
        .alert-info {
            background: linear-gradient(45deg, #d1ecf1, #bee5eb);
            color: #0c5460;
        }
        .alert-warning {
            background: linear-gradient(45deg, #fff3cd, #ffeaa7);
            color: #856404;
        }
        .form-control {
            border-radius: 10px;
            border: 2px solid #e9ecef;
            transition: all 0.3s ease;
        }
        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
    </style>
</head>
<body class="bg-light">
    <%- include('../partials/navbar', { user: user }) %>

    <main class="container mt-4">
        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0"><i class="fas fa-user-circle me-2"></i>환영합니다, <%= user.username %>님!</h5>
                        <span class="badge bg-primary">$<%= user.balance %></span>
                    </div>
                    <div class="card-body">
                        <% if (user.team_name) { %>
                            <p><strong><i class="fas fa-users me-1"></i>팀:</strong> <%= user.team_name %></p>
                        <% } %>
                        
                        <% if (user.role === 'team_leader') { %>
                            <div class="alert alert-info">
                                <strong><i class="fas fa-crown me-1"></i>팀 리더:</strong> 스토어에서 팀을 위한 구매를 할 수 있습니다.
                            </div>
                        <% } %>
                    </div>
                </div>
                
                <div class="card mt-4">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <% if (user.role === 'admin') { %>
                                <i class="fas fa-history me-2"></i>내 최근 거래내역
                            <% } else if (user.team_id) { %>
                                <i class="fas fa-history me-2"></i>팀 최근 거래내역
                            <% } else { %>
                                <i class="fas fa-history me-2"></i>최근 거래내역
                            <% } %>
                        </h5>
                    </div>
                    <div class="card-body">
                        <% if (transactions.length === 0) { %>
                            <div class="text-center py-4">
                                <i class="fas fa-receipt fa-3x text-muted mb-3"></i>
                                <p class="text-muted">아직 거래내역이 없습니다.</p>
                            </div>
                        <% } else { %>
                            <% transactions.forEach(transaction => { %>
                                <div class="transaction-item <%= transaction.type === 'earn' || transaction.type === 'donation_received' ? 'transaction-earn' : 'transaction-spend' %>">
                                    <div class="d-flex justify-content-between">
                                        <div>
                                            <strong><%= transaction.description %></strong>
                                            <% if (user.team_id && user.role !== 'admin' && transaction.username) { %>
                                                <br><small class="text-info">by <%= transaction.username %></small>
                                            <% } %>
                                            <br>
                                            <small class="text-muted">
                                                <%= formatDate(transaction.created_at) %>
                                            </small>
                                        </div>
                                        <div class="text-end">
                                            <span class="<%= transaction.amount > 0 ? 'text-success' : 'text-danger' %>">
                                                <%= transaction.amount > 0 ? '+' : '' %>$<%= Math.abs(transaction.amount) %>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            <% }); %>
                        <% } %>
                    </div>
                </div>
            </div>
            
            <div class="col-md-4">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0"><i class="fas fa-ticket-alt me-2"></i>코드 입력</h5>
                    </div>
                    <div class="card-body">
                        <% if (user.role === 'team_leader' || user.role === 'admin') { %>
                            <div id="redeem-alert"></div>
                            <form id="redeemForm">
                                <div class="mb-3">
                                    <label for="code" class="form-label"><i class="fas fa-coins me-1"></i>머니 코드</label>
                                    <input type="text" class="form-control" id="code" name="code" placeholder="코드를 입력하세요" required>
                                </div>
                                <button type="submit" class="btn btn-success w-100"><i class="fas fa-gift me-2"></i>사용하기</button>
                            </form>
                        <% } else { %>
                            <div class="alert alert-warning">
                                <h6><i class="fas fa-crown me-1"></i>팀 리더 전용</h6>
                                <p class="mb-0">팀 리더만 머니 코드를 사용할 수 있습니다. 팀 리더에게 코드 사용을 요청해주세요.</p>
                            </div>
                        <% } %>
                    </div>
                </div>
                
                <div class="card mt-4">
                    <div class="card-header">
                        <h5 class="mb-0"><i class="fas fa-bolt me-2"></i>빠른 액션</h5>
                    </div>
                    <div class="card-body">
                        <a href="/store" class="btn btn-primary w-100 mb-2"><i class="fas fa-shopping-cart me-2"></i>스토어 방문</a>
                        <a href="/user/team" class="btn btn-outline-primary w-100 mb-2"><i class="fas fa-users me-2"></i>팀 보기</a>
                        <button class="btn btn-outline-secondary w-100" onclick="loadTransactions()"><i class="fas fa-list me-2"></i>모든 거래내역 보기</button>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById('redeemForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const formData = new FormData(e.target);
            const data = Object.fromEntries(formData);
            
            try {
                const response = await fetch('/user/redeem-code', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });
                
                const result = await response.json();
                
                if (result.success) {
                    showRedeemAlert(`성공적으로 $${result.amount}를 받았습니다! 새로운 잔액: $${result.newBalance}`, 'success');
                    document.getElementById('code').value = '';
                    setTimeout(() => location.reload(), 1500);
                } else {
                    showRedeemAlert(result.error, 'danger');
                }
            } catch (error) {
                showRedeemAlert('네트워크 오류가 발생했습니다', 'danger');
            }
        });
        
        function showRedeemAlert(message, type) {
            const alertContainer = document.getElementById('redeem-alert');
            alertContainer.innerHTML = `
                <div class="alert alert-${type} alert-dismissible fade show" role="alert">
                    ${message}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            `;
        }
        
        async function loadTransactions() {
            try {
                const response = await fetch('/user/transactions');
                const result = await response.json();
                
                if (result.transactions) {
                    console.log('All transactions:', result.transactions);
                }
            } catch (error) {
                console.error('Error loading transactions:', error);
            }
        }

        async function logout() {
            try {
                const response = await fetch('/auth/logout', { method: 'POST' });
                if (response.ok) {
                    window.location.href = '/login';
                }
            } catch (error) {
                console.error('Logout error:', error);
            }
        }
    </script>
</body>
</html>